<Page x:Class="DotBotCarClient.Views.EnrollPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Style="{StaticResource TeslaPage}"
      Title="EnrollPage">

    <Grid x:Name="RootGrid">

        <Grid.RenderTransform>
            <TranslateTransform X="40"/>
        </Grid.RenderTransform>

        <Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1" Duration="0:0:0.4"/>
                        <DoubleAnimation Storyboard.TargetProperty="(Grid.RenderTransform).(TranslateTransform.X)"
                                         From="40" To="0" Duration="0:0:0.4"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1"
                Background="{StaticResource TeslaBox}"
                CornerRadius="20"
                Padding="30"
                Width="400"
                Effect="{StaticResource TeslaShadow}">

            <StackPanel>

                <TextBlock Text="회원가입"
                           Style="{StaticResource TeslaTitle}"
                           FontSize="30"/>

                <TextBlock Text="새 계정을 만드세요."
                           Style="{StaticResource TeslaSubText}"
                           Margin="0,0,0,20"/>

                <TextBlock Text="아이디"/>
                <TextBox x:Name="txtId" Height="40" Padding="10"
                         Background="#222" BorderBrush="#444" Foreground="White" Margin="0,0,0,12"/>

                <TextBlock Text="이름"/>
                <TextBox x:Name="txtName" Height="40" Padding="10"
                         Background="#222" BorderBrush="#444" Foreground="White" Margin="0,0,0,12"/>

                <TextBlock Text="비밀번호"/>
                <PasswordBox x:Name="pwBox" Height="40" Padding="10"
                             Background="#222" BorderBrush="#444" Foreground="White" Margin="0,0,0,12"/>

                <TextBlock Text="비밀번호 확인"/>
                <PasswordBox x:Name="pwConfirmBox" Height="40" Padding="10"
                             Background="#222" BorderBrush="#444" Foreground="White" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="BtnEnrollOk" Content="가입 완료"
                            Width="150" Height="42"
                            Click="BtnEnrollOk_Click"
                            Margin="0,0,10,0"/>
                    <Button x:Name="BtnCancel" Content="취소"
                            Width="150" Height="42"
                            Click="BtnCancel_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>
</Page>
